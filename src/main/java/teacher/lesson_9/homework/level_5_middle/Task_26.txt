Создание вспомогательного кода для иерархии Shape (геометрическая фигура).

В классе ShapeUtil реализуйте метод:

    double calculatePerimeter(Shape[] shapes);

Этот метод должен рассчитывать общий периметр
геометрических фигур из переданного в качестве параметра массива.